# STON.fi

STON.fi enables users to pay with supported TON ecosystem tokens.

Architecture:

- quote request
- route preview
- estimated settlement output
- TonConnect swap transaction
- trade tracking
- escrow receipt verification

If `STONFI_API_URL` is missing, the UI and API show setup-required states. Swap success is not faked.
